#!/usr/bin/env bash
set -euo pipefail
# Replace with observed project-native finite build + unit/integration commands.
# Example only: ./gradlew --no-daemon test  OR npm run build && npm test.
printf '%s\n' 'BLOCKED: adapt build.sh to the actual monorepo' >&2
exit 78
