# Адаптация к стеку монорепы
Java: читать Gradle/Maven, transactions, migrations, serialization, nulls, security boundaries; unit/integration отдельно от E2E.
JavaScript/TypeScript: lockfile, framework patterns, async/error handling, schema validation, bundling, UI accessibility; browser E2E для UI.
Go: modules, context cancellation, errors, races, DB transactions; HTTP-client end-to-end против запущенного приложения.
Python: pyproject/lock, sync/async ownership, transactions, typing, fixtures; pytest с mocked service не является E2E.
Команды и зависимости берутся из наблюдаемой версии проекта; skill не закрепляет «самый новый» framework.
