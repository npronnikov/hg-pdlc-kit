# Контракты
HTTP API применим: contracts/openapi.yaml. UI и события интеграции не входят в scope; AsyncAPI неприменим.
priority_changed упомянуто как возможное будущее аналитическое событие, не реализуется в этом change.
