# Решения ВП — демонстрационная фикстура
Не является экспортом human-gate реального HG.
Q allowed_actor → только владелец. Q priority_values → low/normal/high, default normal.
Q version_conflict → 409 с повторным чтением. Q legacy_behavior → id/title сохраняются; UI исключён.
Q success → E2E приемка сейчас; baseline/target продуктовой метрики unknown.
PRD approved в подготовленном сценарии; фактические hash находятся в approval-record.json.
